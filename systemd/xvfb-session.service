[Unit]
Description=X session with terminal on virtual display :99 (console kiểu Proxmox)
After=xvfb.service
Requires=xvfb.service

[Service]
Type=simple
Environment=DISPLAY=:99
# xterm fullscreen - khi thoát bash sẽ restart, hiện terminal mới
ExecStartPre=/bin/sleep 1
ExecStart=/usr/bin/xterm -geometry 1280x720+0+0 -fg white -bg black -fn 10x20 -e /bin/bash -l
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
